[build]
builder = "railpack"
buildCommand = "chmod +x ./build.sh && sh ./build.sh"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$PORT config.wsgi:application"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
